name: Windows Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - run: echo "Publicação temporariamente pausada durante a atualização."
